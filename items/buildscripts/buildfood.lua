function build(directory, config, parameters, level, seed)
  config.maxStack = 20
  config.tooltipKind = ""
  config.rottingMultiplier = -1
  config.itemAgingScripts = ""
  parameters = {}

  return config, parameters
end